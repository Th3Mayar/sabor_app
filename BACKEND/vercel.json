{
  "builds": [{ "src": "./app.ts", "use": "@vercel/node" }],
  "installCommand": "",
  "outputDirectory": "dist",
  "redirects": [
    { "source": "/api/(.*)", "destination": "/api/$1", "statusCode": 200 }
  ]
}
