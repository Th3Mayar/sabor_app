{
  "builds": [
    { "src": "FRONTEND/**", "use": "@vercel/node" },
    { "src": "BACKEND/**", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/FRONTEND/$1" },
    { "src": "/api/(.*)", "dest": "/BACKEND/dist/app.js" }
  ]
}
